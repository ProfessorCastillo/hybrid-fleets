import { ArrowRight, ArrowUpRight, BookOpen, FileText, FlaskConical, Layers3 } from 'lucide-react';
import { sourceUrl } from '../lib';

const propositions = [
  { title: 'Uncertainty needs a buffer.', quote: 'Higher levels of two-sided uncertainty are associated with higher PD capacity needed to minimize total system LMD costs.', explanation: 'A low crowd price does not guarantee capacity when you need it. The paper finds that a dependable private fleet can help absorb fluctuations on both sides.', figure: 'Figure 4 · Base case' },
  { title: 'The relationship can bend.', quote: 'When CD labor supply uncertainty is high, the relationship between demand uncertainty and optimal PD capacity follows a U-shaped curve; when CD labor supply uncertainty is low, the relationship between demand uncertainty and optimal PD capacity is monotonically increasing.', explanation: 'The paper describes a trade-off: at moderate demand variability, the risk of idle private capacity can outweigh the attraction of a larger buffer. The browser implementation is being checked against that published pattern.', figure: 'Figure 4 · High supply variability' },
  { title: 'Shared supply changes the mix.', quote: 'Increased correlation across CD labor supply tiers reduces the optimal PD capacity needed that minimizes total system LMD costs.', explanation: 'The paper studies how shared changes in availability across crowd tiers affect the private-capacity decision. Reproducing this experiment requires matching its individual and common supply distributions and the capacity calculation.', figure: 'Figure 5 · Supply synchronization' },
  { title: 'A missed order has a price.', quote: 'Higher failed delivery costs strengthen the positive relationship between two-sided uncertainty and optimal PD capacity that minimizes total system LMD costs.', explanation: 'For temperature-controlled meals, a failed delivery can mean spoilage, refunds, and redelivery. Increasing that penalty changes the value of having private capacity available.', figure: 'Figure 6 · Failure costs' },
  { title: 'The value is in the combination.', quote: 'Under two-sided uncertainty, a hybrid delivery model combining both PD and CD capacity has lower total expected LMD costs compared to single-source delivery models.', explanation: 'The empirical results favor combining stable private capacity with flexible crowd capacity. The app evaluates each policy on the same days so you can inspect the comparison for your own inputs.', figure: 'Figure S3.1 · Fleet policies' },
];
export default function Research({ methodology, onMethodology, onShow }: { methodology: boolean; onMethodology: () => void; onShow: (n: number) => void }) {
  return <div className="research-page"><section className="research-hero card"><div className="research-mark"><Layers3 size={44}/><span>NVP2S</span></div><div><span className="eyebrow">FROM RESEARCH TO AN INTERACTIVE LAB</span><h2>Last Mile Delivery Capacity Planning<br/>with Two-Sided Uncertainty</h2><p>Castillo, Posner, Sodero, and Zinn · Journal of Business Logistics</p><div className="research-links"><a href={sourceUrl} target="_blank" rel="noreferrer" className="primary-button"><FileText size={15}/> Read the supplied manuscript <ArrowUpRight size={14}/></a><button className="secondary-button" onClick={onMethodology}><FlaskConical size={15}/>{methodology ? 'Explore the propositions' : 'Model & assumptions'}</button></div></div></section>
    {!methodology ? <><div className="research-intro"><div><span className="eyebrow">THE QUESTION</span><h2>How much of your fleet<br/>should you commit to?</h2></div><div><p>The study follows AffordableMeals, an anonymized Brazilian prepared-meals company, as it balances roughly 760 daily orders with uncertain crowdsourced delivery capacity.</p><p>The adapted two-stage newsvendor model weighs private capacity costs, crowd prices, idle savings, reliability, and the cost of a failed delivery. Five propositions capture the paper’s findings.</p></div></div><div className="propositions">{propositions.map((p, i) => <article className="card proposition" key={p.title}><span className="prop-index">P{i + 1}</span><div><span className="eyebrow">{p.figure.toUpperCase()}</span><h2>{p.title}</h2><blockquote>“{p.quote}”</blockquote><p>{p.explanation}</p><button className="text-button" onClick={() => onShow(i + 1)}>Explore the experiment <ArrowRight size={15}/></button></div></article>)}</div></> : <div className="methodology-content"><section className="card prose"><span className="eyebrow">HOW THE MODEL WORKS</span><h2>One decision. Two sources of uncertainty.</h2><div className="method-steps"><div><b>1</b><h3>Describe the market</h3><p>Demand follows a normal distribution. Each crowd tier draws bounded Beta capacity from its own distribution and a shared distribution.</p></div><div><b>2</b><h3>Choose private capacity</h3><p>Use the critical fractile to balance idle capacity costs against expensive crowd tiers and unassigned deliveries.</p></div><div><b>3</b><h3>Allocate each day</h3><p>Use crowd tiers cheaper than private operating cost first, then private capacity, then remaining crowd tiers in ascending cost order.</p></div></div><h3>Reliability-adjusted unit costs</h3><div className="equation" role="math" aria-label="Expected private cost equals private reliability times private cost plus one minus reliability times failed delivery cost">ā = r<sub>priv</sub> a + (1 − r<sub>priv</sub>) u<br/>b̄<sub>i</sub> = r<sub>CD</sub> b<sub>i</sub> + (1 − r<sub>CD</sub>) u</div><h3>The conditional capacity decision · Appendix S1</h3><div className="equation" role="math">H<sub>B</sub>(Q) = Σ<sub>i=0…m</sub> (b̄<sub>i+1</sub> − b̄<sub>i</sub>) F(Q + C<sub>i</sub>) / (u − v)<br/>Q*<sub>B</sub> = min {'{'} Q ≥ 0 : H<sub>B</sub>(Q) ≥ (u − ā) / (u − v) {'}'}</div><p>After cheap-tier supply is removed, F is the remaining-demand CDF, C<sub>i</sub> is cumulative remaining supply, b̄<sub>0</sub> = v, and b̄<sub>m+1</sub> = u. The app chooses the smallest integer that crosses the threshold.</p><h3>The daily cost objective</h3><div className="equation" role="math">G = āQ − v · idle capacity + crowd spend + u · unassigned orders</div><p>This component form equals Appendix S1’s piecewise linear cost expression after adding cheap-tier spend. Reliability is already reflected in expected unit costs. Capacity coverage and map assignments are not estimates of on-time or successful deliveries.</p><h3>A fixed fleet is a different statistic</h3><p>The paper’s simulation solves Q* separately for each supply realization. Its average is shown in Experiments and the distribution chart. The sandbox adds an advance planning calculation: find one Q satisfying E<sub>B</sub>[H<sub>B</sub>(Q)] ≥ (u − ā)/(u − v). This Q stays fixed across simulated days. Averaging conditional optima is generally different from optimizing average cost.</p><p>This fixed-planning extension is an app design choice, not the published Q* recommendation. The cost curve integrates demand analytically for each sampled supply vector. Its band shows supply-driven variation in demand-integrated expected cost. Daily cost confidence intervals describe the simulation mean, not individual future days.</p></section>
    <section className="card prose"><span className="eyebrow">THE ANIMATED ENVIRONMENT</span><h2>A visual explanation of the allocation.</h2><p>Each day on the map uses the demand, supply, and cost allocation from that exact Monte Carlo draw. Private and crowd vehicles represent batches; the number of icons is not a count of drivers. Their weights sum to the model’s allocation totals.</p><p>The road network, delivery destinations, and playback clock are synthetic. The animation illustrates capacity allocation without introducing route optimization, traffic, delivery windows, or additional reliability losses. Playback speed changes the presentation, not the model.</p><h3>Reproducibility and privacy</h3><p>A seeded random stream is forked for every simulated day. The same parameters and seed reproduce the same draws. Runs happen locally in a Web Worker; shared URLs contain your parameters. Saved presets remain in your browser. CSV exports preserve continuous model units and include raw demand and costs.</p><h3>Scope</h3><p>This version includes the sandbox, animated sample days, factorial experiments, all five propositions, and source reconciliation. Historical CSV calibration, 3PL contracting, service-level constraints, and live route planning remain future extensions.</p><div className="research-links"><a href={`${import.meta.env.BASE_URL}sources/IMPLEMENTATION_AUDIT.md`} className="secondary-button" target="_blank" rel="noreferrer">Read the implementation audit <ArrowUpRight size={14}/></a><a href={`${import.meta.env.BASE_URL}sources/DECISIONS.md`} className="secondary-button" target="_blank" rel="noreferrer">Read the implementation assumptions <ArrowUpRight size={14}/></a><a href={`${import.meta.env.BASE_URL}sources/VALIDATION.md`} className="secondary-button" target="_blank" rel="noreferrer">Read the implementation validation report <ArrowUpRight size={14}/></a><a href={`${import.meta.env.BASE_URL}sources/anylogic-model-java.txt`} className="secondary-button" target="_blank" rel="noreferrer">Inspect the supplied Java <ArrowUpRight size={14}/></a></div></section></div>}
    <div className="citation"><BookOpen size={20}/><div><strong>Source attribution</strong><p>Castillo, Posner, Sodero, and Zinn. <em>Last Mile Delivery Capacity Planning with Two-Sided Uncertainty.</em> Journal of Business Logistics. This companion uses the supplied revision manuscript; publication year and DOI were not included in that PDF.</p></div></div></div>;
}
